services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-soundpad}
      POSTGRES_USER: ${POSTGRES_USER:-soundpad}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-soundpad}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

